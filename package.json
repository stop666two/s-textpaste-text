{
  "name": "s-textpaste",
  "version": "1.0.0",
  "private": true,
  "description": "Zero-trust E2E encrypted text sharing with triple-envelope PQ encryption",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "deploy": "node scripts/setup-d1.js && wrangler deploy",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "node worker/server.js"
  },
  "dependencies": {
    "hono": "^4.6.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260608.0",
    "wrangler": "^3.107.0"
  }
}
